<%= form_with(model: listening) do |form| %>
  <% if listening.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(listening.errors.count, "error") %> prohibited this listening from being saved:</h2>

      <ul>
        <% listening.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :level, style: "display: block" %>
    <%= form.text_field :level %>
  </div>

  <div>
    <%= form.label :song_file, style: "display: block" %>
    <%= form.file_field :song_file %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
