<div class="container text-center">
    <p>
        <%= link_to "Admin", admin_path %><%= " > #{params[:action].capitalize}" unless params[:action].eql?("index") %>
    </p>

    <div class="btn-group">
        <%= render 'admin/link', resource: Question %>
        <%= render 'admin/link', resource: Reading %>
        <%= render 'admin/link', resource: Writing %>
        <%= render 'admin/link', resource: Prompt %>
        <%= render 'admin/link', resource: Listening %>
        <%= render 'admin/link', resource: User %>
    </div>
</div>